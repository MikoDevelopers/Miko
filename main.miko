settings (
    "Bot" : "Telegram",
    "Token" : "321",
    "Author" : "123"
)

include (
    "print",
    "dict"
)




typle a = (0, 1, 1);



Bot {
    onMessage(message):
        print("Hew message");
        if (message == "Hello")
        {
            Bot.send(id or username, "World")
        }

    onVoice(message):
        ...
}