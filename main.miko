settings (
    "Bot" : "Telegram",
    "token" : "",
    "author" : ""
)

include (
    ...
)


int a = 12;
int b = 12;



Bot {
    onMessage(message):
        print("Hew message");
        if (message == "Hello")
        {
            Bot.send(id or username, "World")
        }

    onVoice(message):
        ...
}