settings (
    "Bot" : "Telegram",
    "Token" : "321",
    "Author" : "123"
)

include (
    "print",
    "dict"
)


int a = 12;
int b = 12;



Bot {
    onMessage(message):
        print("Hew message");
        if (message == "Hello")
        {
            Bot.send(id or username, "World")
        }

    onVoice(message):
        ...
}